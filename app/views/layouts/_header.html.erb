<nav class="">
    <ul>
        <li> <%= link_to "Store Home", store_path %>
          <% if current_user %>
          | Signed in as <%= current_user.email%>
            <% if current_user.current_cart %>
            | <%= link_to "Cart", cart_path(current_user.current_cart) %></li>
              <% end %>
          <% end %>
          <%= render partial: 'layouts/signin' %>
    </ul>
    <p><%= flash[:notice] %></p>
</nav>
